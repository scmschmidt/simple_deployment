# This is an example for mapping own identifiers to the GCP machine types.
# To get the list of all instance types, run: gcloud compute machine-types list --filter="zone:<zone>"
#
# <label>: <instance type>
e2-micro:    e2-micro
